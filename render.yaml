services:
  - type: web
    name: Meghshree-MeetPro
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --threads 4 -w 1 app:app
    envVars:
      - key: PORT
        value: 10000
    plan: free
